<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Compose new micropost...(140 characters only)", :size => "60x10"  %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
